package fr.da2i.projetcal;

import java.time.LocalDate;

import javax.servlet.http.HttpServletRequest;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.format.annotation.DateTimeFormat;
import org.springframework.stereotype.Controller;
import org.springframework.ui.ModelMap;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.ModelAttribute;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;

@Controller
public class Controleur {
	
	@GetMapping(value="/calendrier")
	public String mois(ModelMap model) {
		return "index";
	}
	
	@GetMapping(value="/calendrier/{moisprecedent}")
	public String moisPrecedent(@PathVariable(required=false) LocalDate moisprecedent ,
			HttpServletRequest request,ModelMap model) {
		moisprecedent  = (LocalDate) request.getAttribute("moisprecedent");
		model.put("moisprecedent", moisprecedent);
		return "index";
	}
}
